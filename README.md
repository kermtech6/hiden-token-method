# hiden-token-method

API Vercel qui expose un `GITHUB_TOKEN` stocké en variable d'environnement,
utilisée par KERM-MD-V1 pour télécharger le repo privé sans exposer le token
dans le code source.

## Structure
